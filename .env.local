# Database (using local SQLite for development)
DATABASE_URL="file:./dev.db"

# Authentication
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="thumbs-consultation-secret-key-2024"

# OAuth Providers (GitHub for free authentication)
GITHUB_CLIENT_ID="your-github-client-id"
GITHUB_CLIENT_SECRET="your-github-client-secret"

# AI Services (OpenRouter free tier)
OPENROUTER_API_KEY="your-openrouter-api-key-when-ready"

# Payment Processing (PayPal sandbox)
NEXT_PUBLIC_PAYPAL_CLIENT_ID="sandbox-paypal-client-id"
PAYPAL_CLIENT_SECRET="sandbox-paypal-secret"

# Email Service (mock for now)
RESEND_API_KEY="mock-email-service"

# App Configuration
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_PAYPAL_ENV="sandbox"
